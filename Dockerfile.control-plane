FROM golang:1.24-alpine AS builder
WORKDIR /app
COPY go.mod ./
COPY . .
RUN go mod download && go mod tidy && CGO_ENABLED=0 go build -o /control-plane ./cmd/control-plane

FROM alpine:3.19
COPY --from=builder /control-plane /control-plane
EXPOSE 8080
CMD ["/control-plane"]
